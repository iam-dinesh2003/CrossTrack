# Production — API goes through CloudFront proxy (/api/* → EB backend)
# Leave empty so axios uses baseURL="/api" (relative = same HTTPS origin)
VITE_API_BASE_URL=
# WebSocket URL for production (CloudFront proxies /ws to EB)
VITE_WS_URL=https://d1um5wttr7o1i.cloudfront.net
